# Python
venv/
__pycache__/
*.pyc
*.pyo

# Environment variables — never commit secrets
.env
.env.*
!.env.example
!.env.*.example

# Local database — each environment has its own
*.db

# macOS
.DS_Store

# Frontend (for when we add React)
node_modules/
dist/
.env.local
